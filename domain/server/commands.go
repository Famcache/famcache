package server

const (
	// CommandSet is the command to set a key
	CommandSet = "SET"

	// CommandGet is the command to get a key
	CommandGet = "GET"

	// CommandGetAll is the command to get all keys
	CommandGetAll = "GETALL"

	// CommandDelete is the command to delete a key
	CommandDelete = "DELETE"
)
